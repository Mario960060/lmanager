/** Dispatched when user selects a sidebar item — pages reset modals/overlays for that section. */
export const LM_SIDEBAR_NAV_EVENT = 'lm-sidebar-nav';

export type SidebarNavDetail = { href: string };
